## 1. Fix KaTeX Dark Mode Color

- [x] 1.1 Add `.katex` to the `text-foreground` override list in `.app-prose` in `src/styles/typography.css`

## 2. Verify

- [x] 2.1 Open the math showcase draft page in dark mode and confirm formulas are visible (light-colored text)
- [x] 2.2 Switch to light mode and confirm formulas still display correctly (no regression)
