## 1. Implement Selection Style

- [x] 1.1 Add `::selection` rule to `src/styles/global.css` with `background-color: color-mix(in srgb, var(--accent) 30%, transparent)` and `color: var(--foreground)`

## 2. Verify

- [x] 2.1 Run `astro dev`, select text on the homepage in light mode and confirm accent-tinted highlight
- [x] 2.2 Switch to dark mode, select text and confirm dark-theme accent highlight
